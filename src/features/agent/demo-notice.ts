/**
 * 給訪客看的示範標示（CR-003）。
 *
 * 放在獨立的檔案而不是 demo-prompt.ts，因為那個檔案是 server-only
 * （系統提示不該進瀏覽器），而這一句話正好相反——它必須出現在畫面上。
 *
 * ⚠️ 必須讓人一眼看出這是示範。讓人以為自己在跟一間真的店講話、
 * 之後才發現店是假的，會讓他連帶懷疑這個網站上其他東西的真假。
 */
export const DEMO_ASSISTANT_NOTICE = "這是示範。它只知道上面這個預覽裡寫的內容。";
