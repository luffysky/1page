# Supabase
#
# 本機開發：執行 `pnpm db:start` 後，CLI 會印出這三個值。
# 雲端專案：Supabase Dashboard → Project Settings → API。

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ⚠️ service role key 可繞過所有 RLS。
# 僅限 server 端使用，絕不可加上 NEXT_PUBLIC_ 前綴，也不可進版控。
SUPABASE_SERVICE_ROLE_KEY=
